# neural-network

Нейронная сеть для распознавания цифр 0–9 на чёрно-белых BMP-изображениях 16×16.

> Ветка `main` содержит исходный проект с 1 курса бакалавриата. Реализация на C++ без внешних библиотек: свёртка, max-pooling, полносвязные слои, ReLU, softmax, обучение методом обратного распространения.

## Требования

- C++17-совместимый компилятор
- CMake 3.10+
- **Windows и Linux** — используется `std::filesystem` для обхода файлов

## Структура проекта

```
neural-network/
├── CMakeLists.txt
├── src/
│   └── main.cpp
├── include/
│   ├── BMP_reading.h      # Чтение BMP
│   ├── functions.h        # Вспомогательные функции
│   ├── matrixFunctions.h  # Операции с матрицами
│   ├── neuralNetwork.h    # Слои и обучение
│   ├── neural_IO.h        # Сохранение/загрузка весов
│   └── testingArea.h      # Тестовые данные
├── cores.dat              # Веса (создаётся при обучении)
└── biases.dat             # Смещения
```

## Сборка

```bash
cmake -B build
cmake --build build
```

## Использование

```bash
./build/neural_network [путь_к_данным] [путь_к_cores] [путь_к_biases] [потоки]
```

**Аргументы (все необязательны):**
- `путь_к_данным` — папка с BMP-изображениями (по умолчанию: `data/`)
- `путь_к_cores` — файл весов для загрузки/сохранения (по умолчанию: `cores.dat`)
- `путь_к_biases` — файл смещений (по умолчанию: `biases.dat`)
- `потоки` — число потоков (инференс: параллельная обработка; обучение: пока последовательное) (по умолчанию: 1)

**Примеры:**
```bash
./build/neural_network                           # data/, cores.dat, biases.dat
./build/neural_network my_images/                # my_images/, cores.dat, biases.dat
./build/neural_network my_images/ model.dat      # + model.dat, biases.dat
./build/neural_network my_images/ model.dat bias.dat
```

1. Создайте папку с обучающими BMP 16×16 (по умолчанию `data/`).
2. **Формат изображений:** монохромный (чёрно-белый) BMP, именно BMP (не PNG с расширением .bmp). Поддерживается 24-битная глубина цвета — 24 бита на пиксель (по 8 бит на R, G, B); код усредняет каналы в оттенки серого.
3. Имена файлов должны начинаться с цифры (метка): например, `0 N.bmp`, `3 N.bmp`, где первая цифра - это непосредственно значение изображения (нужно только для обучения).
4. Запускайте из директории, где находятся папка с данными и файл весов.

При запуске выберите:
- `0` — сгенерировать новые веса и начать обучение
- `1` — загрузить веса из `cores.dat` (можно выбрать только прямой проход для тестирования)

При обучении программа спросит число эпох (по умолчанию 3) и процент датасета для обучения (1–100, по умолчанию 100).

### Датасет

Используется датасет **USPS**: [Kaggle — USPS Dataset](https://www.kaggle.com/datasets/bistaumanga/usps-dataset). Скачайте архив и поместите файл `usps.h5` в папку `data/`. Затем выполните `python3 scripts/usps_h5_to_bmp.py` — появятся папки `data/train/` и `data/test/`. Обучение: `./build/neural_network data/train`. Проверка на тесте: `./build/neural_network data/test` (с загрузкой весов — вариант `1`).

## Архитектура сети

- **Слой 1:** свёрточный, 16 карт признаков, ядро 3×3, ReLU
- **Слой 2:** max-pooling 2×2
- **Слой 3:** flatten
- **Слой 4:** полносвязный, 10 выходов, ReLU
- **Выход:** softmax → предсказание цифры 0–9

## Лицензия

Public domain (Unlicense)
